_version: 0.1

platforms:
  - name: apple
    url: http://apple

social:
  - name: instagram
    url: http://instagram

meta:
  titleTemplate: "%s | {%= data.series[0].title %}"

template:
  layout:
    logoText: "{%= data.series[0].title %}"
  homepage:
    seriesID: "{%= data.series[0].id %}"
